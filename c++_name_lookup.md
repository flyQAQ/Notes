Overload resolution happen before accessibility checking:

- Name lookup
- Overload resolution
- Accessibility checking

As least as a scope is found that contains at least one entity with the given name, the search end.